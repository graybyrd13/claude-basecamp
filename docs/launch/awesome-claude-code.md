# awesome-claude-code PR

Fork https://github.com/hesreallyhim/awesome-claude-code (or the most active equivalent list), add under the most fitting section (Tooling / Dashboards):

```markdown
- [Claude Basecamp](https://github.com/graybyrd13/claude-basecamp) - Zero-dependency localhost dashboard that gives every repo a persistent Claude manager: chat, scheduled routines, background runs with commit linkage, goals, usage stats, notifications, and an MCP server mode.
```

**PR title:** Add Claude Basecamp (project manager dashboard)

**PR body:**

Adds Claude Basecamp — a local, open-source (Unlicense) dashboard for Claude Code. `npx claude-basecamp` reads the session data Claude Code already writes and adds per-repo manager agents, scheduled routines, background runs, notifications, and usage analytics. Zero runtime dependencies, localhost-only.
